# Mini React Feed (Figma-styled)

This is a frontend-only React + TypeScript + Vite project styled to match your Figma screenshot, with Tailwind and Framer Motion.

## Run

```bash
npm install
npm run dev
# open http://localhost:3000
```

## Test accounts

- demo@example.com / password123
- test@user.com / testpass

All data is stored in localStorage — no backend.
